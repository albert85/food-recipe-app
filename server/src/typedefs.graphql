type Query {
 description: String,
 user: [User]
}

type Mutation {
  registerUser(email: String!, password: String!, firstName: String!, lastName: String!, imageUrl: String): User
}

type User {
    id: String,
    firstName: String,
    lastName: String,
    email: String,
    password: String,
    imageUrl: String
}
